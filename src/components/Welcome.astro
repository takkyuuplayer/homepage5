---
import logo from "../assets/logo.svg";

import { useTranslations } from "../i18n/utils";

const lang = Astro.currentLocale;
const t = useTranslations(lang);
---

<div id="container">
	<main>
		<img src={logo.src} width="900" alt={t("logo.alt")} />
	</main>
</div>
